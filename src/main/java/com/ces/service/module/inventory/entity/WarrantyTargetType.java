package com.ces.service.module.inventory.entity;

/** What a warranty extension applies to — a whole product, or one serialized unit of it. */
public enum WarrantyTargetType {
    INVENTORY_ITEM,
    INVENTORY_ITEM_UNIT
}
